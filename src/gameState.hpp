#include <vector>
#include <string>
#include "entity.hpp"


//inline std::vector<std::vector<entity>> tickHistory;



//void storeTickHistory(entity p) {
  //  tickHistory[0].push_back(p);
   // if(tickHistory.size() > 2)
     //   tickHistory.erase(tickHistory.begin() + 2);
//}